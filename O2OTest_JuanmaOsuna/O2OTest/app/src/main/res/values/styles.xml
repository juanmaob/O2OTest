<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppNoBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowContentTransitions">true</item>
    </style>
    <style name="AppNoBarTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppNoBarTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppNoBarTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="TitleLargeTheme">
        <item name="android:textColor">@color/title_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/item_title_large</item>
    </style>

    <style name="TitleTheme">
        <item name="android:textColor">@color/title_text</item>
        <item name="android:textSize">@dimen/item_title</item>
    </style>

</resources>
